service: bot-economy

provider:
  name: aws
  runtime: nodejs12.x

functions:
  bot:
    handler: handler.bot
    events:
      - http:
          path: bot
          method: any
          cors: true

plugins:
  - serverless-plugin-typescript
